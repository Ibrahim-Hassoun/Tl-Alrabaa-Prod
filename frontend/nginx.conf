server {
    listen 3000;  # 👈 Frontend runs on 3000 internally
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;
        index index.html;
    }
}